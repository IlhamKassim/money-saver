# Path is relative to wherever the process starts. Repo root = ./data/finance.db.
DATABASE_URL=./data/finance.db

PORT=3000

# V1 single-profile shortcut. Replace once real auth lands.
DEFAULT_USER_ID=default-user
